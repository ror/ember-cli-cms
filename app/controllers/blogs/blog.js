import Ember from 'ember';
//import moment from 'moment';

export default Ember.Controller.extend({
    //created_at: function(){
    //    return moment(this.get("model.created_at").format("YYYY MMMM Do, h:mm:ss a"));
    //}.property("model")

});
