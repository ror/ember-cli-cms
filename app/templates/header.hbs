<nav class="navbar navbar-default" role="navigation">
    <div class="container">

        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header header">
            <button type="button" class="navbar-toggle" {{action "toggleMobileMenu"}}>
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <div class="wrap">
                <div class="user_img"><img src="/assets/images/logo.png" height="80" alt="website icon"></div>
                <div class="blog_title">ONECOIN.IM</div>
                <div class="write">
                  {{#if session.isAuthenticated}}
                      <a {{ action 'invalidateSession' }}>{{t 'logout'}}</a>
                  {{else}}
                    {{#link-to 'login'}}{{t "login.title"}}{{/link-to}}
                  {{/if}}
                </div>
                <div class="blog_motto">{{t 'header.motto'}}</div>
            </div>
        </div>
    </div>
</nav>
<!-- /.header -->

<div class="menu" id="menu">
    <div class="container">
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav" id="frontMenu">
                <li role="presentation"><a href="/">{{t 'home.title'}}</a></li>

              {{#each item in categories}}
                {{#if item.is_menu}}
                    <li role="presentation">{{#link-to 'blogs.index' (query-params category=item.name) }}
                      {{t item.name }}
                    {{/link-to}}
                    </li>
                {{/if}}
              {{/each}}

                <li role="presentation"><a href="/tools">{{t 'home.tools'}}</a></li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                      {{t 'official.title'}}
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu" role="menu">
                        <li><a href="/official/opportunity">{{t 'official.opportunity'}}</a></li>
                        <li><a href="/official/products">{{t 'official.products'}}</a></li>
                        <li class="divider"></li>
                        <li><a href="/official/about">{{t 'official.about'}}</a></li>
                        <li><a href="http://onecoin.eu/">{{t 'official.home'}}</a></li>
                    </ul>
                </li>

                <li role="presentation"><a href="/about">{{t 'about'}}</a></li>
            </ul>

            <!-- /.navbar-collapse -->
        </div>
    </div>
</div>
